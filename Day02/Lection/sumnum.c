
#include <stdio.h>
int main ()
{
    int num, max;
    scanf("%d", &num);
    max = num;
    while (scanf("%d", &num) != EOF);
    if (num > max)
    {
        
    }
    
     
    printf("%d", num);
    return 0;
}